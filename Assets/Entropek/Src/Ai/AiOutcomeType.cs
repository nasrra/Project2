using UnityEngine;

namespace Entropek.Ai
{
    /// <summary>
    /// A an enum that represents the type of behvaiour an AiOutcome should exhibit when thrown as a callback.
    /// </summary>

    public enum AiOutcomeType : byte
    {
        State,
        Action,
    }    
}

